# Core ingestion pipeline
aiohttp
feedparser
python-dateutil

# Sentiment analysis — install the one you want to use (see sentiment.py)
vaderSentiment          # VaderSentimentAnalyzer  (lightweight fallback)
# transformers          # FinBERTAnalyzer          (recommended — uncomment + install torch)
# torch                 # required by transformers

# Storage backends
motor                   # MongoHandler  (async MongoDB)
pymongo[srv]            # required for mongodb+srv:// Atlas URIs
redis>=4.2              # RedisHandler  (async Redis)
python-dotenv           # loads .env into os.environ at startup
