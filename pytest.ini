[pytest]
# backend modules import each other by bare name (e.g. `import finviz_elite`),
# so put backend/ on sys.path for collection. testpaths keeps pytest from
# wandering into .venv/site-packages tests.
pythonpath = backend
testpaths = backend/tests
addopts = -q
